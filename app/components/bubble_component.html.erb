<div class="<%= align_class %> space-y-1">
  <div class="chat-header flex items-center gap-2 text-xs text-base-content/70">
    <span class="badge badge-ghost badge-sm"><%= badge %></span>
    <span class="font-semibold"><%= label %></span>
  </div>

  <div class="<%= bubble_classes %> whitespace-pre-wrap">
    <%= body %>
  </div>
</div>