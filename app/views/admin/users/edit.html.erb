<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Back to User", admin_user_path(@user), class: "text-blue-600 hover:text-blue-800 font-medium" %>
  </div>

  <h1 class="text-3xl font-bold text-gray-900 mb-6">Edit User</h1>

  <%= render AdminFormComponent.new(model: @user, url: admin_user_path(@user), method: :patch) %>
</div>
