<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Back to Users", admin_users_path, class: "text-blue-600 hover:text-blue-800 font-medium" %>
  </div>

  <h1 class="text-3xl font-bold text-gray-900 mb-6">New User</h1>

  <%= render AdminFormComponent.new(model: @user, url: admin_users_path, method: :post) %>
</div>
